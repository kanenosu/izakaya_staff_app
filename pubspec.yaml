name: izakaya_staff_app
description: "居酒屋スタッフ向け注文・在庫・メニュー管理アプリ（Flutter + Firebase）"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  cloud_firestore: any
  video_player: ^2.8.2
  cupertino_icons: ^1.0.8
  flutter_staggered_grid_view: ^0.7.0
  intl: ^0.17.0
  record: ^5.1.2
  http: ^1.2.1
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
